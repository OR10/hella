class InnerDocumentVisitor {
  visitDocumentBefore(firstNode, secondNode, path) {
  }

  visitDocumentAfter(firstNode, secondNode, path) {
  }

  visitScalar(firstNode, secondNode, path) {
  }

  visitArrayBefore(firstNode, secondNode, path) {
  }

  visitArrayAfter(firstNode, secondNode, path) {
  }

  visitObjectBefore(firstNode, secondNode, path) {
  }

  visitObjectAfter(firstNode, secondNode, path) {
  }
}

export default InnerDocumentVisitor;