import angularMocks from 'angular-mocks';
import $ from 'jquery';
import angular from 'angular';

describe('UnitTestDummy', function() {
  it('should fail', function() {
    expect(1).toBe(2);
  });

  it('should pass', function() {
    expect(1).toBe(1);
  });
});
