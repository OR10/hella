---
:backends:
    - yaml
:hierarchy:
    - "node/%{::fqdn}"
    - "vagrant/%{::annostation_project}/%{::vagrant_environment}"
    - "vagrant/%{::vagrant_environment}/%{::annostation_servertype}"
    - "vagrant/%{::vagrant_environment}"
    - "%{::environment}/%{::annostation_project}/%{::annostation_servertype}"
    - "%{::environment}/%{::annostation_project}"
    - "servertype/%{::annostation_project}/%{::annostation_servertype}"
    - "%{::environment}/%{::annostation_servertype}"
    - "servertype/%{::annostation_servertype}"
    - "%{::environment}"
    - global
:yaml:
  :datadir: puppet/hiera
